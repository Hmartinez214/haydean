# Never commit the cluster admin password.
# README instructs you to place PASSWORD.txt in this directory before running
# the scripts — this rule makes sure it cannot be committed by accident.
PASSWORD.txt

# Slurm RPMs built by build_slurm_rpms.sh
slurm-rpms/
*.rpm

# Generated by generate_slurm_conf.sh; regenerate rather than commit
# (uncomment if you prefer not to track the generated config)
#slurm.conf
